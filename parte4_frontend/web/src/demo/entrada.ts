/**
 * Punto de entrada del modo demostración (`vite.demo.config.ts` lo pone en lugar de `src/main.tsx`): primero el
 * sustituto del BFF y el aviso, después la aplicación de siempre, sin cambios.
 */
import './instalar'
import './aviso'
import '@/main'
